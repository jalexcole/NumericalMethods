

fun cramersRule(matrix: Array<Array<Double>>, b: Array<Double>): Array<Double> {
    // Assumes a 2d matrix


    return arrayOf()
}

fun determinate(matrix: Array<Array<Double>>): Double {


    return 0.0
}

fun checkIfSquare(matrix: Array<Array<Double>>): Boolean {
    val x = matrix.size
    val y = matrix[0].size
    return x == y
}